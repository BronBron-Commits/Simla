# Simla Examples

Run any example:

```bash
npm run simla -- examples/<file>.sim
```

Good starting points:

* `examples/hello.sim` for the smallest runnable Simla program
* `examples/first_scene.sim` for a minimal scene-shaped result
* `examples/cube_scene.sim` for a richer animated scene
