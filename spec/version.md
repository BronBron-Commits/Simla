# Simla Versioning

Current version: 0.1.0

Core guarantees:
- Deterministic execution
- Cross-runtime parity (JS, C interpreter, C bytecode)
- Integer + list semantics
- Functional primitives (fn, map, reduce, etc.)
